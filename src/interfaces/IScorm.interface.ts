import { Request, Response } from 'express';

// the format of users 'GET' request
export interface ScormUploadRequest extends Request {
    
}

// the format of the response of a user 'GET' request
export interface ScormUploadResponse extends Response {
    
}
  